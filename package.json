{
  "name": "gluniverse-titan-world",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --preserve-symlinks --preserve-symlinks-main scripts/build-packs.mjs",
    "test": "node --preserve-symlinks --preserve-symlinks-main --test tests/*.test.mjs",
    "check": "node --preserve-symlinks --preserve-symlinks-main scripts/check.mjs"
  }
}
